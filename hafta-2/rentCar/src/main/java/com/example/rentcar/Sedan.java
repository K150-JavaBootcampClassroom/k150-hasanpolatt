package com.example.rentcar;

public class Sedan extends Car {
    public Sedan(String brand, String model, int modelYear, String color, int baggage) {
        super(brand, model, modelYear, color, baggage);
    }
}
